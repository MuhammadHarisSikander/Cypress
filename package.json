{
  "scripts": {
    "cypress:open": "cypress open",
    "cy:open": "npx cypress open --env allure=true --browser chrome",
    "cy:run": "npx cypress run --env SUITE=Haris --config video=false --env allure=true --browser chrome",
    "allure:report": "allure generate allure-results --clean -o allure-report",
    "smoke": "npx cypress run ",
    "regression": "",
    "test": "npm run cy:run && npm run allure:report && allure serve ",
    "cy:clientLogin": "cypress run --record --key 1c65c639-9209-4c79-a65a-c578cc4bb3ad --browser chrome --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean allure-report && allure open  allure-report",
    "test:allure": "npm run cy:clientLogin && npm run report:allure"
  },
  "devDependencies": {
    "@cypress/grep": "^3.1.0",
    "@shelex/cypress-allure-plugin": "^2.34.0",
    "allure-commandline": "^2.20.1",
    "cypress": "^10.6.0",
    "cypress-grep": "^3.0.3",
    "cypress-if": "^1.10.2",
    "cypress-xpath": "^2.0.1",
    "mocha-allure-reporter": "^1.4.0"
  }
}